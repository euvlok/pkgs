From 7204499e4c24be9b6c265adaf5d04c98454da1f9 Mon Sep 17 00:00:00 2001
From: FlameFlag <git@ps1.sh>
Date: Fri, 24 Apr 2026 18:51:20 +0300
Subject: [PATCH] Add richer status line command telemetry

---
 codex-rs/tui/src/chatwidget.rs                | 12 +++
 .../tui/src/chatwidget/status_surfaces.rs     | 99 ++++++++++++++++++-
 codex-rs/tui/src/chatwidget/tests/helpers.rs  |  1 +
 codex-rs/tui/src/status/rate_limits.rs        | 10 +-
 4 files changed, 118 insertions(+), 4 deletions(-)

diff --git a/codex-rs/tui/src/chatwidget.rs b/codex-rs/tui/src/chatwidget.rs
index 34c2e21f2..f99ead574 100644
--- a/codex-rs/tui/src/chatwidget.rs
+++ b/codex-rs/tui/src/chatwidget.rs
@@ -388,6 +388,7 @@ mod side;
 mod status_surfaces;
 use self::status_surfaces::CachedProjectRootName;
 use self::status_surfaces::TerminalTitleStatusKind;
+use self::status_surfaces::status_line_diff_stats_from_unified_diff;
 use crate::streaming::chunking::AdaptiveChunkingPolicy;
 use crate::streaming::commit_tick::CommitTickScope;
 use crate::streaming::commit_tick::run_commit_tick;
@@ -761,6 +762,12 @@ impl PendingGuardianReviewStatus {
     }
 }
 
+#[derive(Clone, Copy, Debug, Default, Eq, PartialEq)]
+pub(crate) struct StatusLineDiffStats {
+    pub(crate) added: u64,
+    pub(crate) removed: u64,
+}
+
 /// Maintains the per-session UI state and interaction state machines for the chat screen.
 ///
 /// `ChatWidget` owns the state derived from the protocol event stream (history cells, streaming
@@ -805,6 +812,7 @@ pub(crate) struct ChatWidget {
     status_account_display: Option<StatusAccountDisplay>,
     token_info: Option<TokenUsageInfo>,
     rate_limit_snapshots_by_limit_id: BTreeMap<String, RateLimitSnapshotDisplay>,
+    status_line_diff_stats: Option<StatusLineDiffStats>,
     refreshing_status_outputs: Vec<(u64, StatusHistoryHandle)>,
     next_status_refresh_request_id: u64,
     plan_type: Option<PlanType>,
@@ -2520,6 +2528,7 @@ impl ChatWidget {
         self.adaptive_chunking.reset();
         self.plan_stream_controller = None;
         self.turn_runtime_metrics = RuntimeMetricsSummary::default();
+        self.status_line_diff_stats = None;
         self.session_telemetry.reset_runtime_metrics();
         self.bottom_pane.clear_quit_shortcut_hint();
         self.quit_shortcut_expires_at = None;
@@ -4387,6 +4396,8 @@ impl ChatWidget {
 
     fn on_turn_diff(&mut self, unified_diff: String) {
         debug!("TurnDiffEvent: {unified_diff}");
+        let stats = status_line_diff_stats_from_unified_diff(&unified_diff);
+        self.status_line_diff_stats = (stats.added > 0 || stats.removed > 0).then_some(stats);
         self.refresh_status_line();
     }
 
@@ -5190,6 +5201,7 @@ impl ChatWidget {
             status_account_display,
             token_info: None,
             rate_limit_snapshots_by_limit_id: BTreeMap::new(),
+            status_line_diff_stats: None,
             refreshing_status_outputs: Vec::new(),
             next_status_refresh_request_id: 0,
             plan_type: initial_plan_type,
diff --git a/codex-rs/tui/src/chatwidget/status_surfaces.rs b/codex-rs/tui/src/chatwidget/status_surfaces.rs
index 03ef3281f..6d4965045 100644
--- a/codex-rs/tui/src/chatwidget/status_surfaces.rs
+++ b/codex-rs/tui/src/chatwidget/status_surfaces.rs
@@ -838,6 +838,15 @@ struct StatusLineCommandRateLimits {
     seven_day: StatusLineCommandRateLimitWindow,
 }
 
+#[derive(Serialize)]
+struct StatusLineCommandCost {
+    total_cost_usd: Option<f64>,
+    total_duration_ms: Option<u64>,
+    total_api_duration_ms: Option<u64>,
+    total_lines_added: Option<u64>,
+    total_lines_removed: Option<u64>,
+}
+
 #[derive(Serialize)]
 struct StatusLineCommandPayload {
     workspace: StatusLineCommandWorkspace,
@@ -846,6 +855,22 @@ struct StatusLineCommandPayload {
     model: StatusLineCommandModel,
     context_window: StatusLineCommandContextWindow,
     rate_limits: StatusLineCommandRateLimits,
+    cost: StatusLineCommandCost,
+}
+
+pub(super) fn status_line_diff_stats_from_unified_diff(diff: &str) -> StatusLineDiffStats {
+    let mut stats = StatusLineDiffStats::default();
+    for line in diff.lines() {
+        if line.starts_with("+++") || line.starts_with("---") {
+            continue;
+        }
+        if line.starts_with('+') {
+            stats.added = stats.added.saturating_add(1);
+        } else if line.starts_with('-') {
+            stats.removed = stats.removed.saturating_add(1);
+        }
+    }
+    stats
 }
 
 fn build_status_line_command_payload(chat: &ChatWidget) -> String {
@@ -884,15 +909,26 @@ fn build_status_line_command_payload(chat: &ChatWidget) -> String {
                 used_percentage: limit_snapshot
                     .and_then(|snapshot| snapshot.primary.as_ref())
                     .map(|window| window.used_percent),
-                resets_at: None,
+                resets_at: limit_snapshot
+                    .and_then(|snapshot| snapshot.primary.as_ref())
+                    .and_then(|window| window.resets_at_unix),
             },
             seven_day: StatusLineCommandRateLimitWindow {
                 used_percentage: limit_snapshot
                     .and_then(|snapshot| snapshot.secondary.as_ref())
                     .map(|window| window.used_percent),
-                resets_at: None,
+                resets_at: limit_snapshot
+                    .and_then(|snapshot| snapshot.secondary.as_ref())
+                    .and_then(|window| window.resets_at_unix),
             },
         },
+        cost: StatusLineCommandCost {
+            total_cost_usd: None,
+            total_duration_ms: None,
+            total_api_duration_ms: None,
+            total_lines_added: chat.status_line_diff_stats.map(|stats| stats.added),
+            total_lines_removed: chat.status_line_diff_stats.map(|stats| stats.removed),
+        },
     };
 
     serde_json::to_string(&payload).unwrap_or_else(|_| "{}".to_string())
@@ -992,6 +1028,30 @@ mod tests {
             last_token_usage: TokenUsage::default(),
             model_context_window: Some(272_000),
         });
+        chat.rate_limit_snapshots_by_limit_id.insert(
+            "codex".to_string(),
+            RateLimitSnapshotDisplay {
+                limit_name: "codex".to_string(),
+                captured_at: chrono::Local::now(),
+                primary: Some(RateLimitWindowDisplay {
+                    used_percent: 16.0,
+                    resets_at: Some("soon".to_string()),
+                    resets_at_unix: Some(1_700_000_000),
+                    window_minutes: Some(300),
+                }),
+                secondary: Some(RateLimitWindowDisplay {
+                    used_percent: 45.0,
+                    resets_at: Some("later".to_string()),
+                    resets_at_unix: Some(1_700_086_400),
+                    window_minutes: Some(10_080),
+                }),
+                credits: None,
+            },
+        );
+        chat.status_line_diff_stats = Some(StatusLineDiffStats {
+            added: 42,
+            removed: 7,
+        });
 
         let payload = build_status_line_command_payload(&chat);
         let value: serde_json::Value = serde_json::from_str(&payload).expect("payload json");
@@ -1007,6 +1067,41 @@ mod tests {
             value["context_window"]["current_usage"]["cache_read_input_tokens"],
             25
         );
+        assert_eq!(value["rate_limits"]["five_hour"]["used_percentage"], 16.0);
+        assert_eq!(
+            value["rate_limits"]["five_hour"]["resets_at"],
+            1_700_000_000
+        );
+        assert_eq!(value["rate_limits"]["seven_day"]["used_percentage"], 45.0);
+        assert_eq!(
+            value["rate_limits"]["seven_day"]["resets_at"],
+            1_700_086_400
+        );
+        assert_eq!(value["cost"]["total_cost_usd"], serde_json::Value::Null);
+        assert_eq!(value["cost"]["total_lines_added"], 42);
+        assert_eq!(value["cost"]["total_lines_removed"], 7);
+    }
+
+    #[test]
+    fn status_line_diff_stats_ignores_diff_headers() {
+        let diff = "\
+diff --git a/file.txt b/file.txt
+--- a/file.txt
++++ b/file.txt
+@@ -1,3 +1,4 @@
+ unchanged
+-old
++new
++added
+";
+
+        assert_eq!(
+            status_line_diff_stats_from_unified_diff(diff),
+            StatusLineDiffStats {
+                added: 2,
+                removed: 1,
+            }
+        );
     }
 
     #[tokio::test]
diff --git a/codex-rs/tui/src/chatwidget/tests/helpers.rs b/codex-rs/tui/src/chatwidget/tests/helpers.rs
index 23ba3812d..ac75c4a16 100644
--- a/codex-rs/tui/src/chatwidget/tests/helpers.rs
+++ b/codex-rs/tui/src/chatwidget/tests/helpers.rs
@@ -200,6 +200,7 @@ pub(crate) async fn make_chatwidget_manual(
         status_account_display: None,
         token_info: None,
         rate_limit_snapshots_by_limit_id: BTreeMap::new(),
+        status_line_diff_stats: None,
         refreshing_status_outputs: Vec::new(),
         next_status_refresh_request_id: 0,
         plan_type: None,
diff --git a/codex-rs/tui/src/status/rate_limits.rs b/codex-rs/tui/src/status/rate_limits.rs
index 559da21e8..82078c327 100644
--- a/codex-rs/tui/src/status/rate_limits.rs
+++ b/codex-rs/tui/src/status/rate_limits.rs
@@ -66,14 +66,16 @@ pub(crate) struct RateLimitWindowDisplay {
     pub used_percent: f64,
     /// Human-readable local reset time.
     pub resets_at: Option<String>,
+    /// Raw reset timestamp, as Unix seconds, when provided by the server.
+    pub resets_at_unix: Option<i64>,
     /// Window length in minutes when provided by the server.
     pub window_minutes: Option<i64>,
 }
 
 impl RateLimitWindowDisplay {
     fn from_window(window: &RateLimitWindow, captured_at: DateTime<Local>) -> Self {
-        let resets_at_utc = window
-            .resets_at
+        let resets_at_unix = window.resets_at;
+        let resets_at_utc = resets_at_unix
             .and_then(|seconds| DateTime::<Utc>::from_timestamp(seconds, 0))
             .map(|dt| dt.with_timezone(&Local));
         let resets_at = resets_at_utc.map(|dt| format_reset_timestamp(dt, captured_at));
@@ -81,6 +83,7 @@ impl RateLimitWindowDisplay {
         Self {
             used_percent: window.used_percent,
             resets_at,
+            resets_at_unix,
             window_minutes: window.window_minutes,
         }
     }
@@ -358,6 +361,7 @@ mod tests {
         RateLimitWindowDisplay {
             used_percent,
             resets_at: Some("soon".to_string()),
+            resets_at_unix: Some(1_700_000_000),
             window_minutes: Some(300),
         }
     }
@@ -415,11 +419,13 @@ mod tests {
             primary: Some(RateLimitWindowDisplay {
                 used_percent: 20.0,
                 resets_at: Some("soon".to_string()),
+                resets_at_unix: Some(1_700_000_000),
                 window_minutes: Some(60),
             }),
             secondary: Some(RateLimitWindowDisplay {
                 used_percent: 40.0,
                 resets_at: Some("later".to_string()),
+                resets_at_unix: Some(1_700_003_600),
                 window_minutes: None,
             }),
             credits: None,
-- 
2.53.0

